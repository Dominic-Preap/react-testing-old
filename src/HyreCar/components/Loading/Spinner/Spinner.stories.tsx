import * as React from 'react';

import { storiesOf } from '@storybook/react';

import { Spinner } from './Spinner';

storiesOf('HyreCar|Components/Loading', module).add('Spinner', () => <Spinner />);
